spring.application.name: AiVoice
spring:

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: mysql
    url: jdbc:mysql://localhost:3306/local_database
    username: root
    password: 123456
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

logging:
  level:
    org:
      springframework:
        web:
          DEBUG
server:
  port: 8989

